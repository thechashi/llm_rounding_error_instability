from setuptools import setup, find_packages

setup(
    name="llm_rounding_error_instability",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[
        # Add your project dependencies here
    ],
)